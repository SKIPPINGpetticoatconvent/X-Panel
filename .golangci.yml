# golangci-lint v2 configuration
# Reference: https://golangci-lint.run/docs/configuration/
version: "2"

run:
  tests: false
  timeout: 5m

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gosec


  exclusions:
    paths:
      - test_scripts

issues: {}
