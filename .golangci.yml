# golangci-lint v2 configuration
# Reference: https://golangci-lint.run/docs/configuration/


run:
  tests: false
  timeout: 5m

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused

  exclusions:
    paths:
      - test_scripts

issues: {}
